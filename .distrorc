#!/bin/sh

source ~/.config/fluhzar/.commonrc

DIR="$HOME/.config/$USER"
DISTRO=$($DIR/sbin/print_distro)
DISTRO_PREFIX="$DIR/."
DISTRO_SUFFIX="rc"

DISTRO_RC="$DISTRO_PREFIX$DISTRO$DISTRO_SUFFIX"

source $DISTRO_RC
